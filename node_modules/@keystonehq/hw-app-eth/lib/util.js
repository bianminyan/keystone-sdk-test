"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertCompresskey = void 0;
const elliptic_1 = __importDefault(require("elliptic"));
const convertCompresskey = (compressedPublicKey) => {
    const ec = new elliptic_1.default.ec('secp256k1');
    // Convert the compressed public key to a key pair object
    const key = ec.keyFromPublic(compressedPublicKey, 'hex');
    // Get the uncompressed public key
    let uncompressed = key.getPublic();
    return uncompressed.encode('hex', false);
};
exports.convertCompresskey = convertCompresskey;
//# sourceMappingURL=util.js.map