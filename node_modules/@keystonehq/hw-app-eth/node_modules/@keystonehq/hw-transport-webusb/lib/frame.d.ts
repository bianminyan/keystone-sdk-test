import { Buffer } from 'buffer';
import { Actions } from './actions';
import { Status } from '@keystonehq/hw-transport-error';
/**
 * Encode the data into packets
 * @param command
 * @param requestID
 * @param strData
 * @returns
 */
export declare const encode: (command: Actions, requestID: number, data: string | Uint8Array) => Uint8Array[];
/**
 * Decode the packets into data
 * @param buffers
 * @returns
 */
export declare const decode: (buffers: Buffer[]) => {
    data: string;
    status?: Status;
};
//# sourceMappingURL=frame.d.ts.map